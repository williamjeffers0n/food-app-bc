/**
 * enum for status can accept
 */
export enum Status {
    PLACED = 'placed',
    ACKNOWLEDGE = 'acknowledge',
    READY = 'ready',
    DELIVERED = 'delivered',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
}
